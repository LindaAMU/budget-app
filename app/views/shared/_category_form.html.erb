<%= simple_form_for @category do |f| %>
  <%= f.input :name, placeholder: "Add the category's name" %>
  <%= f.input :category_type, placeholder: "Is an expenses or an income?", as: :select  %>
  <%= f.button :submit, "Create Category" %>
<% end %>
